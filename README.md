# SQL-query-practice
A collection of 25 SQL practice queries covering key database concepts such as joins, aggregations, subqueries, filtering, grouping, pattern matching, sorting, and pagination (LIMIT/OFFSET).
